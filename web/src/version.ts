/**
 * App release label shown in the UI. On each push/deploy: bump the patch segment
 * (1.0.0 → 1.0.1) and set the date to that day (YYYY-MM-DD).
 */
export const APP_VERSION = "1.0.1";
export const APP_VERSION_DATE = "2026-03-25";

export const APP_VERSION_DISPLAY = `v${APP_VERSION}-${APP_VERSION_DATE}`;
